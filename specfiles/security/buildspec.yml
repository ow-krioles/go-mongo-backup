version: 0.2

phases:
  install:
    commands:
      - echo "Install phase"
  pre_build:
    commands:
      - echo "Pre-Build phase"
  build:
    commands:
      - echo "Build phase"
  post_build:
    commands:
      - echo "Post-Build phase"
